apply 	-> prasino-aspro
decline	-> kokkino-aspro
rest 	-> mple-aspro

news-feed will be larger

TeamDetails needs arrays everywhere

profile->add photo setting

Difficulty rating -> already-rate = one star with (x/5)
		  -> not-defined rate as it is
		  -> atprojectDetail before comments

TeamDetails	  -> add_member-leave_team-submit
close/open group  -> lock/unlock icon

profil		  -> eye/not eye

some_buttons name not good(example,decline -> decline instead of send)

scrollbars not needed, we have the whole screen

comments/history different color per modulo 2(like table)

close/open projects at different rank
